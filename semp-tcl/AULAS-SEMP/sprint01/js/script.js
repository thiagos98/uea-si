/*var query = location.search.slice(1);
var partes = query.split('&');
var data = {};
partes.forEach(function (parte) {
    var chaveValor = parte.split('=');
    var chave = chaveValor[0];
    var valor = chaveValor[1];
    data[chave] = valor;
});

window.onload = function gets(){
  var nome = data['nome'];
  var curso = data['curso'];
  var universidade = data['universidade'];
  //var data = data['data'];
  var profissional = data['profissional'];
  var academico = data['academico'];
  document.getElementById('nome').innerHTML = nome;
  document.getElementById('curso').innerHTML = curso;
  document.getElementById('universidade').innerHTML = universidade;
  document.getElementById('profissional').innerHTML = profissional;
  document.getElementById('academico').innerHTML = academico;
}
gets();
*/
