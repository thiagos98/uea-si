/*Escreva uma função recursiva divide(A, B) para calcular a divisão inteira de 
	A por B utilizando apenas subtrações*/
	
#include <stdio.h>

int divisao(int a,int b){
	
}

int main(int argc, char** argv){
	int a,b,res=0;
	printf("Dividendo: ");
	scanf("%d",&a);
	printf("Divisor: ");
	scanf("%d",&b);
	//res = divisao(a,b);
	return 0;
}